/**
 * SimulatorPage — SKELETON (placeholder).
 * Later: replay recorded data as if it were live.
 */
export default function SimulatorPage() {
  return (
    <div className="page">
      <h1 className="page-title">Simulator</h1>
      <p className="page-subtitle">
        Replays recorded data as if live — ideal for testing without a car. (In progress)
      </p>
      <div className="card" style={{ color: 'var(--text-2)', textAlign: 'center', padding: '60px 20px' }}>
        🎛 Simulator — the replay stream will appear here.
      </div>
    </div>
  );
}
